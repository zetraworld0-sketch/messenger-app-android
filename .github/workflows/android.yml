name: Build Netscape APK

on:
  push:
    branches:
      - master
      - main
  workflow_dispatch:

jobs:
  build:
    name: Generate Netscape Baseline
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant Execute Permission
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug --no-daemon -Pandroid.unitTests.ignoreTestFailures

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: netscape-baseline-apk
          path: app/build/outputs/apk/debug/*.apk
          retention-days: 7
