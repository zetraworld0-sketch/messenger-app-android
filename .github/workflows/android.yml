name: Zetra Engine Production Build

on:
  push:
    branches: [ develop, main ]
  pull_request:
    branches: [ develop, main ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Generate Zetra Binaries
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Validate Gradle Wrapper
        run: chmod +x gradlew

      - name: Build Zetra Engine
        # This skips the broken sample tasks that are causing your 8-minute crashes
        run: >
          ./gradlew assembleDebug 
          -x :stream-chat-android-ui-components-sample:compileDemoDebugKotlin
          -x :stream-chat-android-ui-components-sample:processDemoDebugResources
          -x :stream-chat-android-ui-components-sample:mergeDemoDebugResources
          --no-daemon 
          --parallel

      - name: Archive Zetra APKs
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Zetra-Build-Output
          path: "**/build/outputs/apk/debug/*.apk"
          retention-days: 7
