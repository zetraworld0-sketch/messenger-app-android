<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/register_new_profile_main_content"
    android:background="@color/md_white_1000"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/register_new_profile_appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="0dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <!-- Added a Toolbar to maintain brand authority during registration -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/registration_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="Create Netscape Identity"
            app:titleTextColor="@color/white"
            android:background="@color/myPrimaryColor" />

    </com.google.android.material.appbar.AppBarLayout>

    <FrameLayout
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:id="@+id/register_new_profile_fragment_place"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        
        <!-- Fragments for phone and profile will be injected here -->
        
    </FrameLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
